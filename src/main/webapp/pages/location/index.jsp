<html>
<head>
    <title>Dynamics +Gain: ${location.name}</title>
</head>
<body>

    <p>Please help!</p>

    <h1>${location.name}</h1>
    <h2><strong class="highlight">${location.count}</strong>&nbsp; in need.</h2>
    <p>${location.description}</p>

    <h3>Give to ${location.name}</h3>
    <p>You can make a one-time or a reoccurring donation that goes
        directly to ${location.name}.</p>
    <a href="/z/donate/locations/${location.id}" class="button super yellow">Give Now &hearts;</a>

</body>
</html>
